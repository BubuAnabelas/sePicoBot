const NotasStyles = theme => ({
  root: {
    height: `calc(100vh - 64px)`,
    marginTop: theme.spacing(8)
  },
  tableContainer: {
    marginTop: theme.spacing(8)
  },
  grow: {
    flexGrow: 1
  },
  boxCard: {
    padding: '2px',
    height: '100%'
  }
});

export default NotasStyles;
