const dashboardStiles = theme => ({
  shortcutCard: {
    height: theme.spacing(19),
    cursor: 'pointer'
  },
  contentContainer: {
    marginTop: theme.spacing(5),
    marginBottom: theme.spacing(5)
  }
});

export default dashboardStiles;
